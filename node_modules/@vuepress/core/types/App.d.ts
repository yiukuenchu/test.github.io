import { ThemeAPI } from './Theme'
import { PluginAPI } from './Plugin'
import { SiteConfig } from './SiteConfig'
import { Page } from './Page'

export interface InstantiationOptions {
  /**
   * Source directory of your source documents.
   */
  sourceDir?: string;
  /**
   * Location of temporary files.
   */
  temp?: string;
  /**
   * Site config.
   */
  siteConfig?: SiteConfig;

}

export declare class VuePressApp {
  constructor (options: InstantiationOptions);

  /**
   * process.cwd()
   */
  cwd: string;
  /**
   * Source directory of your source documents.
   */
  sourceDir: string;
  /**
   * Site config.
   */
  siteConfig: SiteConfig;
  /**
   * Theme config.
   */
  themeConfig: Record<string, any>;
  /**
   * Normalized output directory under build.
   */
  outDir: string;
  /**
   * siteConfig.base || '/'
   */
  base: string;
  /**
   * `${sourceDir}/.vuepress`.
   */
  vuepressDir: string;
  /**
   * Root directory of `@`vuepress/core/lib`.
   */
  libDir: string;
  /**
   * Interface working with theme.
   */
  themeAPI: ThemeAPI;
  /**
   * Interface working with plugin.
   */
  pluginAPI: PluginAPI;
  /**
   * All available pages.
   */
  pages: Array<Page>;
}

export class VuePressContext extends VuePressApp {
  /**
   * Write temporary file and return file path.
   */
  writeTemp (file: string, content: string): string;
}
